### Automation task document

I attempted to automate several endpoints and user flows. Some parts did not work as expected — for example, listing a newly added email. Also, tests on the home page sometimes fail because the UI does not update quickly enough for Playwright; this is a common source of flakiness.

---
