[pytest]
# Override global addopts from project pyproject.toml when running tests in this
# directory. Leaving addopts empty prevents pytest from injecting Django-specific
# options like --ds and --reuse-db for Playwright (E2E) tests.
addopts = -n 2

# Keep default discovery pattern (optional)
python_files = test_*.py
